{
  "name": "exploringu",
  "private": true,
  "description": "ExploringU — career clarity assessment (React + Django).",
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
